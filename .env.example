# Shared bearer token between agents and the master, and for the browser UI.
# Generate something with: python -c "import secrets; print(secrets.token_urlsafe(32))"
CEREBRO_TOKEN=change-me

# Optional: server log level (DEBUG / INFO / WARNING)
LOG_LEVEL=INFO
